C:/MyProject/Portfolio/
│
├── app.py                  # [메인] 웹 서버 실행 파일 (대문 역할)
├── config.py               # [설정] DB 비밀번호, API 키 등 통합 관리
├── requirements.txt        # [설정] 설치된 라이브러리 목록
│
├── templates/              # [대문 UI] 메인 페이지 HTML 폴더
│   └── index.html          # "내 포트폴리오에 오신 것을 환영합니다" (프로젝트 선택 화면)
│
├── static/                 # [공통] CSS, 이미지, JS 파일
│   ├── css/
│   └── img/
│
├── my_hotdeal/                # [Project 1] 핫딜 알리미 (현재 작업 중)
│   ├── __init__.py         # 패키지 설정 (Blueprint 등록)
│   ├── routes.py           # 핫딜 관련 페이지 접속 경로 (URL) 관리
│   ├── crawler.py          # (기존 파일 이동) 크롤링 로직
│   ├── database.py         # DB 연결 및 쿼리 함수 모음
│   └── templates/          # 핫딜 프로젝트 전용 HTML
│       ├── list.html       # 핫딜 목록
│       └── my_page.html    # 내 보관함
│
└── ai_safety/              # [Project 2] AI 객체 탐지 (다음 예정)
    ├── __init__.py
    ├── detector.py         # 객체 탐지 로직 (YOLO 등)
    └── templates/          # AI 프로젝트 전용 HTML
        └── monitor.html    # CCTV 모니터링 화면

# git remote add origin https://github.com/HuBiZuk/Portfolio.git

# flask 구조 : 개발자가 자유롭게 정하는 구조
